type Hotel = {
    id: string;
    name: string;
    address: string;
    rating: number;
    amenities: string[];
    pricePerNight: number;
    images: string[];
    description: string;
};

export default Hotel;